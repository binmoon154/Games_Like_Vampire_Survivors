
# 뱀서라이크 게임 PRD

## 1. 문서 목적

플레이어가 캐릭터를 직접 이동시키고, 공격은 자동으로 발동되는 방식의 2D 생존 액션 게임을 제작한다. 짧은 시간 안에 조작법을 이해할 수 있으면서도, 적을 처치하고 얻은 경험치로 무기와 능력을 조합하는 재미를 제공한다.

이 문서는 게임의 첫 번째 플레이 가능한 버전에 필요한 규칙, 콘텐츠, 화면, 기술 요구사항과 개발 순서를 정의한다.

## 2. 게임 개요

- 장르: 2D 탑다운 생존 액션, 로그라이트
- 플랫폼: PC 및 모바일 웹 브라우저
- 권장 기술: HTML, CSS, JavaScript Canvas 등등
- 플레이 방식: 1인 플레이
- 1회 플레이 시간: 15분
- 핵심 조작: 방향키 또는 WASD로 이동, 공격은 자동
- 승리 조건: 제한 시간 동안 생존하고 최종 보스를 처치
- 패배 조건: 플레이어의 체력이 0이 됨
- 핵심 재미: 이동, 자동 공격, 적 무리 회피, 경험치 성장, 무기 조합

### MVP 범위

다음 기능은 MVP에서 제외하고 이후 버전에서 구현한다.

- 무기 시너지 및 무기 진화 시스템
- 키 재설정과 색맹 모드 등 접근성 옵션
- 여러 캐릭터와 캐릭터별 시작 무기
- 게임패드 지원

단, 광과민성 문제를 줄이기 위해 과도한 화면 흔들림과 번쩍임은 MVP부터 사용하지 않는다.

## 3. 핵심 게임 루프

1. 게임을 시작하고 플레이어를 조작한다.
2. 적이 사방에서 생성되어 플레이어를 추적한다.
3. 플레이어의 무기가 일정한 규칙에 따라 자동으로 공격한다.
4. 적을 처치하면 경험치 보석이 떨어진다.
5. 경험치를 모아 레벨업하면 3개의 능력 중 하나를 선택한다.
6. 시간이 지날수록 적의 수, 체력, 이동 속도가 증가한다.
7. 마지막에 등장하는 보스를 처치하면 승리한다.
8. 마지막 보스를 죽이면 승리하기는 하지만 게임이 바로 끝나지는 않고 더 플레이 할 수 있음, 이때는 정예 몬스터가 몰려오며 죽어야 게임이 끝남(이때 죽인 몬스터도 점수에 포함)
9. 게임 종료 후 결과를 보여주고 다시 시작할 수 있게 한다.

## 4. 플레이어

### 기본 능력치

| 능력치 | 설명 | 초기값 예시 |
| --- | --- | --- |
| 체력 | 0이 되면 게임 오버 | 100 |
| 이동 속도 | 초당 이동 거리 | 180 |
| 공격력 | 무기가 주는 기본 피해량 | 무기별 설정 |
| 공격 범위 | 무기가 적에게 영향을 줄 수 있는 거리 | 무기별 설정 |
| 공격 속도 | 공격 사이의 시간 | 무기별 설정 |
| 행운 | 아이템 및 선택지의 확률에 영향 | 0% |

### 조작

- `W`, `A`, `S`, `D` 또는 방향키: 이동
- `Esc`: 일시정지
- 메뉴와 레벨업 선택: 마우스 클릭 또는 숫자키 `1`, `2`, `3`
- 이동 중 화면 가장자리를 넘어가지 않는다.
- 이동 방향에 따라 캐릭터의 좌우 방향을 뒤집을 수 있다.

### 모바일 터치 조작

- 화면 왼쪽 아래에 고정형 가상 조이스틱을 표시한다.
- 조이스틱의 중심에서 손가락이 이동한 방향과 거리를 이동 벡터로 사용한다.
- 조이스틱의 최대 입력 거리를 이동 속도 100%로 간주하고, 손가락을 놓으면 이동을 멈춘다.
- 화면 오른쪽 아래에는 일시정지 버튼을 배치한다.
- 레벨업 선택지는 화면 전체를 덮는 터치 가능한 카드로 표시한다.
- 터치 조작 영역은 최소 44px 이상으로 만들고, 게임 화면을 가리지 않도록 투명도를 적용한다.
- `touchstart`, `touchmove`, `touchend`에서 `preventDefault()`를 적용해 화면 스크롤과 우발적인 확대를 막는다.
- 모바일에서는 브라우저 주소창 변화에 대응해 Canvas 크기를 다시 계산하고 세로·가로 화면 모두에서 UI가 겹치지 않게 한다.
- 조이스틱을 조작 중인 손가락의 `touch.identifier`를 저장해 멀티터치를 구분한다. 다른 손가락으로 일시정지 버튼을 눌러도 조이스틱 이동 방향에는 영향을 주지 않는다.

### 행운 스탯 적용 방식

- 회복 아이템 드롭 확률 = 기본 확률 × (1 + 행운 / 100)
- 레벨업 선택지에 희귀 능력이 등장할 확률에도 같은 공식을 적용한다.
- 이 수치는 초안이므로 플레이테스트 결과에 따라 조정한다.

## 5. 무기와 공격 시스템

공격은 자동으로 실행한다. 플레이어는 공격 대상을 직접 선택하지 않고 이동과 빌드 선택에 집중한다.
공격은 자동으로 하지만, 플레이어 기준으로 앞으로 나가는 공격과 자동으로 적을 공격하는 공격이 있다.

### 무기  - 추가 필요

#### 레벨 1 무기 수치 초안

| 무기 | 피해량 | 공격 주기 | 사거리 | 투사체 속도 | 관통 | 비고 |
| --- | ---: | --- | --- | --- | --- | --- |
| 마법 탄환 | 10 | 0.9초 | 400px | 500px/s | 1마리 | 가장 가까운 적 자동 조준 |
| 회전 수리검 | 6 | 접촉 후 0.3초 | 반경 70px | 해당 없음 | 제한 없음 | 회전 속도 180도/초 |
| 번개 | 18 | 2.0초 | 화면 내 랜덤 | 즉시 | 1마리 | 주기마다 1마리 타격 |

#### 무기 레벨 강화 초안

- 마법 탄환: Lv2는 투사체 2개 동시 발사, Lv3은 공격 주기를 0.7초로 단축한다.
- 회전 수리검: Lv2는 반경을 90px로 증가, Lv3은 회전 속도를 240도/초로 증가한다.
- 번개: Lv2는 동시 타격을 2마리로 증가, Lv3은 피해량을 18에서 26으로 증가한다.

1. **마법 탄환**
	- 가장 가까운 적을 향해 탄환 발사
	- 단일 대상 공격
	- 탄환 속도와 공격 횟수 업그레이드 가능
2. **회전 수리검**
	- 플레이어 주변을 회전하며 접촉한 적에게 피해
	- 공격 범위와 회전 속도 업그레이드 가능
3. **번개**
	- 일정 주기마다 임의의 적에게 즉시 피해
	- 공격 횟수와 피해량 업그레이드 가능

### 공격 공통 규칙

- 무기마다 공격 주기, 피해량, 사거리, 투사체 수, 관통 횟수를 가진다.
- 같은 무기를 다시 선택하면 해당 무기를 강화한다.
- 무기는 최대 6개까지 장착할 수 있다.
- 적중, 처치, 치명타 등 주요 전투 효과는 시각 효과와 간단한 효과음으로 전달한다.

## 6. 적과 보스

### 시간대별 적 능력치 증가율 초안

- 기준 시점은 게임 시작 후 0분이다.
- 적 체력은 매 60초마다 8%씩 복리로 증가한다.
- 적 이동 속도는 매 120초마다 5%씩 증가하며, 최대 40%까지만 증가한다.
- 적 생성 주기는 0~2분 2.0초, 2~5분 1.2초, 5~10분 0.8초, 10분 이후 0.5초로 설정한다.
- 정예 적은 7분부터 일반 적 생성 시 5% 확률로 대체되며, 11분 이후에는 12%로 증가한다.

### 적 종류 - 추가 필요

| 적 | 특징 | 등장 시점 |
| --- | --- | --- |
| 추적자 | 느리지만 기본적인 근접 공격을 함 | 시작부터 |
| 돌진자 | 잠시 멈춘 뒤 빠르게 접근함 | 3분 이후 |
| 원거리 적 | 거리를 유지하며 투사체를 발사함 | 4분 이후 |
| 정예 적 | 체력이 높고 처치 시 경험치를 많이 줌 | 7분 이후 |
| 사신 | 닿은 플레이어를 즉사 시킴 | 15분에 나오는 보스를 잡고 플레이어가 1분 이상 버티면 나옴 |





### 적 공통 규칙

- 적은 화면 바깥에서 생성되어 플레이어를 향해 이동한다.
- 적끼리 겹치지 않도록 밀어내거나 간단한 충돌 처리를 한다.
- 적에게 접촉하면 플레이어가 피해를 입는다.
- 피해를 입은 뒤 짧은 무적 시간을 제공해 연속 피해를 방지한다.
- 적 처치 시 경험치 보석과 낮은 확률의 회복 아이템을 떨어뜨린다.

### 적 집단 행동 규칙

- 각 적은 플레이어를 향한 추적 힘과 주변 적을 밀어내는 분리 힘을 함께 계산한다.
- 플레이어와 가장 가까운 적은 정면에서 접근하고, 나머지 적은 플레이어 주변의 빈 각도와 거리로 분산한다.
- 플레이어 반경 120px 안에 적이 6마리 이상 있으면 일부 적은 좌우 측면으로 이동해 포위 대형을 만든다.
- 같은 무리의 적은 0.5초마다 목표 위치를 갱신하며, 서로 겹치지 않도록 최소 24px의 간격을 유지한다.
- 원거리 적은 플레이어와 220~300px 거리를 유지하고, 근접 적 무리 뒤에서 공격한다.
- 돌진자는 다른 적과 일정 거리를 유지한 뒤 플레이어가 있는 방향으로 0.8초 동안 돌진하고, 돌진 후 1.2초 동안 멈춘다.
- 정예 적은 주변 일반 적을 밀어내고, 일정 범위 안의 적에게 이동 속도 보너스를 제공한다.
- 정예 적 반경 150px 안의 일반 적에게 이동 속도 15% 보너스를 제공한다. 여러 정예 적의 범위가 겹쳐도 보너스는 중첩하지 않는다.
- 화면에 적이 너무 많아지면 새 적 생성을 일시적으로 제한하되, 이미 생성된 적의 행동은 계속 진행한다.
- 동시에 살아 있을 수 있는 적은 250마리로 제한한다. 250마리에 도달하면 생성을 멈추고, 10마리 이하로 줄어들면 생성을 재개한다.

### 보스

- 플레이 시간 5분, 10분, 15분에 각각 다른 보스가 등장한다.
- 보스는 일반 적보다 크고 체력이 높다.
- 보스는 등장 후 2초 동안 무적이며, 등장 위치와 경고 효과를 화면에 표시한다.
- 보스의 공격 패턴은 텔레그래프(공격 범위와 발동 예정 시간을 미리 보여주는 효과)를 먼저 표시한다.

#### 5분 보스: 돌격 대장

- **직선 돌진**: 0.8초 동안 이동 방향을 고정하고 플레이어를 향해 돌진한다. 돌진 경로를 붉은 선으로 0.6초 미리 표시하며, 벽이나 화면 경계에 부딪히면 1초 동안 기절한다.
- **부채꼴 충격파**: 2초마다 전방 90도 범위에 3개의 충격파를 발사한다. 충격파 사이의 빈 공간으로 피할 수 있다.

#### 10분 보스: 소환술사

- **원형 광역 장판**: 플레이어 주변에 반지름 80px의 원형 공격 범위를 표시한 뒤 1초 후 폭발한다. 폭발 후 장판은 사라진다.
- **적 소환**: 8초마다 추적자 6마리를 보스 주변에 소환한다. 소환 중에는 보스가 1.5초 동안 이동하지 않는다.

#### 15분 최종 보스: 이무기

- **독액 분사**: 플레이어의 현재 위치를 기준으로 0.7초 뒤 독액을 직선으로 내뿜는데, 발사 경로는 미리 붉은 선으로 표시되고 화면 끝까지 이동한다. 맞으면 지속 피해를 입을 뿐 아니라 짧은 시간 동안 이동 속도가 느려지는 중독 효과가 함께 걸립니다.
- **여의주 낙뢰**: 체력이 30퍼센트 이하로 떨어지면 이무기가 승천을 시도하며 5초마다 플레이어 주변에 여의주처럼 빛나는 원형 경고 범위를 반지름 110픽셀로 만들고, 1.2초 뒤 그 자리에 벼락이 떨어집니다. 벼락이 떨어질 때 그 범위 안에 있으면 아주 큰 피해를 입으므로, 기존 즉사 영역과 동일하게 플레이어는 그 시간 안에 범위 밖으로 빠져나가야 합니다.

#### 보스 체력 초안

| 보스 | 체력 | 설계 의도 |
| --- | ---: | --- |
| 돌격 대장(5분) | 800 | 일반 추적자 체력의 약 40배 |
| 소환술사(10분) | 1,800 | 소환된 적과 함께 상대 |
| 이무기(15분) | 3,500 | 체력보다 패턴 회피가 핵심 |

## 7. 성장과 아이템

### 경험치와 레벨업

- 적을 처치하면 경험치 보석이 생성된다.
- 플레이어가 가까이 가면 경험치 보석이 끌려온다.
- 경험치 게이지가 가득 차면 게임을 일시정지하고 능력 선택 화면을 연다.
- 3개의 선택지를 보여주며 하나를 선택하면 게임을 재개한다.
- 다음 레벨에 필요한 경험치는 이전 레벨보다 증가한다.

#### 경험치 곡선 초안

```text
다음 레벨 필요 경험치 = 20 + (현재 레벨 × 15) + (현재 레벨^1.4 × 2)
```

- 레벨 1→2는 약 37, 레벨 10→11은 약 220, 레벨 20→21은 약 470을 목표로 한다.
- 일반 적 경험치 보석은 추적자 1~2, 돌진자 2~3, 원거리 적 3, 정예 적 15~20으로 설정한다.
- 위 값은 초안이며 실제 플레이테스트를 통해 조정한다.

### 능력 선택 예시

- 최대 체력 증가
- 이동 속도 증가
- 공격력 증가
- 공격 속도 증가
- 경험치 획득 범위 증가
- 회복 효과
- 신규 무기 획득

### 아이템

- 경험치 보석: 레벨업에 필요한 경험치를 제공
- 고기: 체력을 14% 회복
- 구급상자: 체력을 34% 회복
- 자석: 일정 시간 동안 화면의 경험치 보석을 끌어옴
- 폭탄: 화면 안의 적에게 큰 피해를 줌

## 8. 스테이지와 난이도

- 화면은 플레이어를 중심으로 보이는 제한된 전투 영역으로 구성한다.
- 배경에는 바닥 타일, 장애물 또는 장식물을 배치한다.
- 시간이 지날수록 적 생성 주기를 줄이고 적의 능력치를 높인다.
- 시간대별로 등장 적과 이벤트를 관리한다.

| 시간 | 진행 내용 |
| --- | --- |
| 0~2분 | 기본 추적자와 기본 무기로 게임 규칙 학습 |
| 2~5분 | 적 생성량 증가 |
| 5분 | 첫 보스가 등장 |
| 5분 | 돌진자와 원거리 적 추가 |
| 8~10분 | 정예 적 등장, 적 능력치 크게 증가 |
| 10분 | 보스 등장 |
| 11분 | 적 강화, 정예 적 등장 생성 시간 감소와 수 증가 |
| 15분 | 보스 등장 |
| 보스 잡은 후 | 보스를 잡으면 게임이 바로 끝나지 않고, 정예 적들이 강화 된 체 몰려오며 사신이 나타나 다가와서 죽일려함 |


## 9. 화면 및 UI

### 필요한 화면

1. **시작 화면**
	- 게임 제목
	- 시작 버튼
	- 조작법 버튼 또는 간단한 조작 안내
2. **게임 화면**
	- 플레이어, 적, 투사체, 아이템
	- 체력 바
	- 경험치 바와 현재 레벨
	- 생존 시간
	- 현재 무기와 능력 아이콘
3. **레벨업 화면**
	- 게임 일시정지
	- 능력 선택 카드 3개
	- 능력 이름, 효과, 현재 레벨 표시
4. **일시정지 화면**
	- 계속하기
	- 다시 시작
	- 시작 화면으로 돌아가기
5. **게임 오버 및 승리 화면**
	- 결과 메시지
	- 생존 시간
	- 처치한 적 수
	- 도달 레벨
	- 처치한 보스 수와 마지막으로 처치한 보스
	- 획득한 경험치와 점수
	- 다시 하기 버튼

### 최고 기록 저장

- `localStorage`에 최고 점수, 최장 생존 시간, 최고 도달 레벨을 각각 저장한다.
- 게임 오버 또는 승리 화면에서 현재 결과가 최고 기록을 갱신했는지 표시한다.
- 저장 키는 `vs_bestScore`, `vs_bestSurvivalTime`, `vs_bestLevel`을 사용한다.

## 10. 시각 및 오디오 방향

- 2D 픽셀 아트 또는 단순하고 명확한 도형 그래픽을 사용한다.
- 플레이어, 적, 경험치, 투사체는 색상과 형태만으로 구분 가능해야 한다.
- 전투 효과는 짧고 명확하게 표시하되 화면을 가리지 않게 한다.
- MVP에서는 무료 사용이 가능한 이미지와 효과음을 사용하거나 직접 제작한다.
- 필요한 오디오: 공격음, 적 피격음, 적 처치음, 레벨업음, 보스 등장음, 게임 오버음.

### 오디오 구현 방식

- 효과음은 Web Audio API를 사용한다. 모바일에서 발생하는 재생 지연을 줄이기 위해 짧은 효과음은 오디오 노드로 재생한다.
- 같은 효과음의 동시 재생 수는 효과음 종류별 최대 4개로 제한하며, 초과 요청은 무시한다.
- 배경음은 별도 채널로 재생해 효과음과 독립적으로 관리한다. 볼륨 조절 UI는 MVP 범위에서 제외하지만 코드 구조는 분리한다.

## 11. 기술 요구사항

### 게임 구조

- `requestAnimationFrame`을 사용해 게임 루프를 실행한다.
- 매 프레임마다 입력, 이동, 충돌, 공격, 적 생성, 아이템, UI를 갱신한다.
- 렌더링과 게임 로직을 분리해 기능을 수정하기 쉽게 한다.
- 게임 일시정지 중에는 게임 로직과 타이머가 진행되지 않게 한다.
- 프레임 속도가 달라도 이동 속도가 일정하도록 delta time을 사용한다.
- 게임 로직과 렌더링에 사용하는 `deltaTime`은 한 프레임당 최대 50ms로 제한한다.

### 권장 데이터 구조

- `gameState`: 현재 화면, 일시정지 여부, 생존 시간(`survivalTime`), 점수, 현재 스테이지, 보스 처치 수, 승리 조건 진행 상태
- `player`: 위치, 체력, 능력치, 레벨, 경험치, 장착 무기
- `enemies`: 적 목록과 적별 위치, 체력, 속도, 공격 정보, 집단 행동 그룹 ID, 현재 행동 상태
- `projectiles`: 투사체 목록과 이동 및 충돌 정보
- `items`: 경험치 보석, 회복 아이템 등의 목록
- `weapons`: 무기 정의, 레벨별 수치, 공격 함수
- `statistics`: 처치한 일반 적 수(`kills`), 처치한 보스 수(`bossKills`), 획득 경험치(`totalExperience`), 생존 시간(`survivalTime`), 도달 레벨(`level`), 받은 피해량
- 적을 처치할 때마다 해당 적의 종류와 관계없이 `statistics.kills`를 1 증가시킨다.
- 보스를 처치할 때는 `statistics.bossKills`를 1 증가시키고, `gameState.lastDefeatedBoss`에 보스 ID를 기록한다.
- 게임 오버 또는 승리 시 `statistics`를 읽어 결과 화면의 처치 수, 생존 시간, 레벨, 점수를 표시한다.
- `pool`: 적 300개, 투사체 200개, 경험치 보석 400개의 초기 객체 풀. 풀이 부족하면 동적으로 확장하되 각 종류별 상한을 둔다.

### 성능 기준

- 일반적인 노트북 브라우저에서 많은 적이 등장해도 게임이 플레이 가능해야 한다.
- 화면 밖의 적과 아이템은 필요에 따라 정리한다.
- 매 프레임 불필요한 DOM 생성 대신 Canvas 렌더링을 사용한다.
- 충돌 판정은 원형 또는 사각형 기반의 단순한 방식으로 시작한다.

### 성능 최적화 기법

- 게임 로직과 렌더링에 동일한 `deltaTime`을 사용하되, 한 프레임의 `deltaTime`은 최대 50ms로 제한해 창을 오래 숨겼다가 돌아왔을 때 순간이동과 대량 연산이 발생하지 않게 한다.
- 적, 투사체, 경험치 보석은 객체 풀을 사용해 생성과 삭제를 반복하지 않는다. 처치되거나 화면 밖으로 나간 객체는 비활성화한 뒤 재사용한다.
- 적과 투사체의 충돌 판정은 모든 객체를 서로 비교하지 않는다. 화면을 일정한 크기의 격자 셀로 나누고 같은 셀 또는 인접 셀에 있는 객체만 후보로 검사한다.
- 격자 셀의 시작 크기는 100px × 100px로 설정하고 Performance 패널 측정 결과에 따라 조정한다.
- 화면 밖에 있는 장식물은 렌더링하지 않고, 플레이어에게서 일정 거리 이상 떨어진 적은 AI 목표 갱신 빈도를 낮춘다.
- 적 집단의 분리 힘과 포위 목표 위치는 매 프레임이 아니라 0.1초 간격으로 갱신한다. 이동과 충돌은 매 프레임 처리한다.
- Canvas의 실제 해상도와 CSS 표시 크기를 분리해 고해상도 모바일 기기에서도 최대 픽셀 비율을 2로 제한한다.
- 매 프레임마다 DOM을 생성하거나 스타일을 변경하지 않는다. 체력·경험치·시간 UI는 필요한 값이 바뀔 때만 갱신한다.
- 파티클과 피해 숫자는 화면에 표시할 최대 개수를 제한하고, 오래된 효과부터 제거한다.
- 파티클은 최대 150개, 피해 숫자는 최대 40개까지만 동시에 표시한다.
- 적 생성 수에는 상한을 두며, `enemies.length`가 상한에 도달하면 생성 타이머를 일시 중지한다.
- 개발 중에는 브라우저의 Performance 패널로 프레임 시간, 충돌 판정 시간, 렌더링 시간을 확인한다. 일반 플레이 중 평균 50 FPS 이상, 적이 가장 많은 구간에서도 30 FPS 이상을 목표로 한다.

## 12. MVP 완료 기준

다음 항목을 모두 만족하면 첫 번째 버전이 완성된 것으로 본다.

- 시작 화면에서 게임을 시작할 수 있다.
- WASD 또는 방향키로 플레이어를 움직일 수 있다.
- 적이 지속적으로 생성되고 플레이어를 추적한다.
- 최소 1개의 무기가 자동으로 공격한다.
- 투사체 또는 공격이 적에게 피해를 주고 적을 처치한다.
- 처치한 적이 경험치 보석을 떨어뜨린다.
- 경험치를 모으면 레벨업 선택지가 표시된다.
- 능력을 선택하면 실제 플레이 능력치가 변경된다.
- 플레이어가 피해를 받고 체력이 0이면 게임 오버가 된다.
- 일정 시간이 지나면 보스가 등장한다.
- 보스 처치 시 승리 화면이 표시된다.
- 일시정지, 다시 시작, 게임 오버 후 재시작이 정상 작동한다.
- 데스크톱 브라우저에서 키보드 조작으로 오류 없이 플레이할 수 있다.
- 모바일 브라우저에서 가상 조이스틱으로 이동하고 터치 카드로 레벨업을 선택할 수 있다.
- 게임 오버 및 승리 화면에 `kills`, `bossKills`, `survivalTime`, 도달 레벨과 점수가 정확히 표시된다.
- 게임 오버 또는 승리 시 `localStorage`의 최고 기록과 비교해 기록 갱신 여부가 표시된다.

## 13. 개발 순서

### 1단계: 기본 실행

- HTML에 Canvas와 기본 UI 구성
- 게임 시작 및 재시작 상태 구현
- 플레이어 표시와 WASD/방향키 입력 구현
- 게임 루프와 delta time 구현

### 2단계: 전투 프로토타입

- 적 생성과 플레이어 추적 구현
- 플레이어 자동 공격 구현
- 투사체와 적의 충돌 판정 구현
- 적 처치와 체력 감소 구현

### 3단계: 성장 시스템

- 경험치 보석 생성 및 획득 구현
- 경험치 바와 레벨업 구현
- 3개 능력 선택 화면 구현
- 무기 및 능력 강화 데이터 연결

### 4단계: 게임 진행과 결과

- 시간별 난이도 조절
- 적 종류 추가
- 보스와 보스 등장 연출 구현
- 승리, 패배, 일시정지 화면 구현

### 5단계: 완성도 향상

- 그래픽, 애니메이션, 파티클 효과 추가
- 효과음과 배경음 추가
- 밸런스 조정 및 버그 수정
- 데스크톱 및 모바일 화면 크기 테스트

## 14. 테스트 체크리스트

- 게임을 시작하고 정상적으로 이동할 수 있는가?
- 키를 동시에 누르거나 키를 빠르게 반복해도 이동이 멈추지 않는가?
- 적이 화면 밖에서 생성되고 플레이어를 향해 이동하는가?
- 적이 너무 많이 쌓여도 브라우저가 멈추지 않는가?
- 적들이 서로 겹치지 않고 측면으로 분산되어 플레이어를 포위할 수 있는가?
- 돌진자와 보스 공격의 경고 영역이 실제 공격보다 먼저 표시되는가?
- 투사체가 적을 맞힐 때 피해와 처치가 정확한가?
- 경험치 획득 후 레벨업 선택 화면이 한 번만 나타나는가?
- 레벨업 중 적과 시간이 멈추는가?
- 체력이 0일 때 게임 오버 화면이 표시되는가?
- 보스가 정해진 시간에 등장하고 처치할 수 있는가?
- 보스 처치 수와 일반 적 처치 수가 각각 정확히 기록되는가?
- 다시 시작했을 때 이전 게임의 적, 아이템, 능력치가 남지 않는가?
- 창 크기를 바꿔도 Canvas와 UI가 겹치지 않는가?
- 모바일 세로·가로 화면에서 가상 조이스틱, 일시정지 버튼, 레벨업 카드가 겹치지 않는가?
- 손가락을 조이스틱에서 떼면 캐릭터가 즉시 이동을 멈추고 페이지가 스크롤되지 않는가?
- 조이스틱 조작 중 다른 손가락으로 화면을 터치해도 이동 방향이 흐트러지지 않는가?
- Performance 패널에서 적이 가장 많은 구간에도 목표 FPS를 유지하는가?
- 새로고침 후에도 이전 최고 기록이 유지되는가?
- 여러 효과음이 동시에 재생되어도 오디오가 끊기거나 브라우저가 버벅이지 않는가?

## 15. 이후 확장 아이디어

- 여러 캐릭터와 캐릭터별 시작 무기
- 영구 성장 및 해금 시스템
- 여러 스테이지와 환경 효과
- 무기 진화 조합
- 일일 도전과 서버 기반 기록 저장
- 키 설정과 접근성 옵션(색맹 모드, 화면 흔들림 끄기 포함)
- 게임패드 지원
















